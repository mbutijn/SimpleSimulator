function vector_out = unwrap2(vector_in, margin)

differences = diff(vector_in);
vector_out = vector_in;

for i = 1:length(differences)
    if differences(i) > margin
        vector_out(i+1:end) = vector_in(i+1:end) - 2*pi;
    end
end